export function add(a:number, b:number) : number {
    return a + b;
}

export default class Test {

    /**
     * @param a
     * @param b
     */
    public add(a:number,b:number) {
       return a + b;
    }
}